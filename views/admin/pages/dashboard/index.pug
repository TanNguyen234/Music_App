extends ../../layouts/default.pug

block main 
    h1 Welcome to the #{title}
    p Welcome to the #{description} website!

    if showFeaturedArticle
        article
            h2 #{featuredArticle.title}
            p #{featuredArticle.content}